# 보안 및 개인정보 범위

- 무단 사이트 점검이나 대량 크롤링 용도로 사용하지 않습니다. 사용자가 선택한 현재 탭 또는 제공한 HTML만 검사합니다.
- 확장 프로그램은 activeTab, scripting, storage만 사용하며 cookies/debugger/전체 사이트 host 권한은 요구하지 않습니다.
- 사용자가 붙여넣은 HTML은 원본 스크립트·이벤트 핸들러·외부 리소스 연결을 제거한 뒤, same-origin 권한이 없는 CSP 샌드박스에서 검사합니다.
- 검사 결과의 HTML/문자열은 textContent로 렌더링합니다. HTML 내보내기에는 문자 이스케이프와 기본 네트워크 차단 CSP를 사용합니다.
- 입력값과 인증정보를 수집하지 않지만 URL 경로와 DOM 이름·id·class·ARIA에는 개인정보가 포함될 수 있습니다.
- 외부 서버로 검사 결과를 전송하지 않습니다. 브라우저·다운로드 결과 파일의 보안은 사용자가 관리해야 합니다.
- `public/downloads`와 `docs/`는 공개 배포용입니다. 실제 사이트 보고서를 이 경로에 저장하지 마세요.
- 의존성 버전은 고정되어 있으나 향후 보안 업데이트·회귀 테스트가 필요합니다.
- 데모는 명시적으로 오류가 있는 테스트 데이터입니다. 데모의 잘못된 마크업을 제품 코드로 복사하지 마세요.
