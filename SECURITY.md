# 개인정보와 검사 안전성

자동 외부 전송·쿠키 읽기·비밀번호 수집을 하지 않습니다. 확장은 activeTab/scripting/storage만 사용하며 전체 사이트 상시 권한을 요구하지 않습니다.

v0.2.0 보고서는 오류를 이해할 수 있도록 자식 마크업, ID·class·ARIA·레이블과 화면 텍스트를 보존합니다. 입력값, 실행 코드, data 속성 값, URL 인증정보·쿼리는 제외합니다. 이것이 완전한 익명화를 의미하지는 않습니다. 화면 텍스트나 속성에도 개인정보가 있을 수 있으므로 JSON/HTML 공유 전 확인하세요.

확장 보고서는 브라우저 로컬 저장소에 저장됩니다. 온라인 화면은 세션 저장소를 사용합니다. 공유 PC에서는 검사 후 저장 보고서를 삭제하세요.

실제 화면 캡처는 명시적 확인 후에만 수행합니다. 현재 보이는 대상 영역만 잘라 저장하고 입력 필드를 가립니다. 화면 텍스트까지 자동 익명화하지 않습니다. 캡처 전에 활성 탭과 검사 식별자를 확인하며 원본 요소가 변경되면 재검사를 요구합니다.

HTML 미리보기는 네트워크·외부 리소스·폼 제출을 차단한 iframe에서 격리합니다. 증거 미리보기는 스크립트 권한조차 부여하지 않습니다. 보고서 코드와 가져온 문자열은 textContent 또는 이스케이프된 코드로 표시합니다.

실제 스크린리더 실행부는 데스크톱 로컬 도구이며 공개 URL 크롤링 서버가 아닙니다. 준비된 페이지에서 탐색 명령만 실행합니다. 발화·스크린샷·접근성 트리에는 개인정보가 남을 수 있습니다. 기본적으로 스크린샷은 저장하지 않습니다.
