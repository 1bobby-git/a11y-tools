# a11y-tools 배포 준비본

## 실행

Windows에서 `DEPLOY-WINDOWS.cmd`를 실행합니다. Git와 GitHub CLI가 필요하며, 브라우저 인증 이후 `PUBLISH`를 입력합니다. 대상은 기존의 `1bobby-git/a11y-tools` 공개 저장소입니다. 새 저장소 생성이나 강제 푸시는 수행하지 않습니다.

## 이 대화에서의 실제 상태

- 저장소 조회: 성공. 공개 저장소, 기본 브랜치 main, 조회 당시 size 0.
- 계정 권한: push/admin 권한 확인. 계정 권한 부족을 원인으로 판정하지 않음.
- 연결된 도구: 읽기 API만 제공하며 원격 쓰기 동작을 호출할 수 없음.
- 터미널: GitHub CLI와 Git 인증 없음. GitHub 및 npm 호스트 이름 해석 실패.
- 따라서 원격 커밋, 업로드, Pages 설정, 실제 웹 게시: 미수행.

## 수정 범위

핵심 검사 코드는 유지했습니다. 공개 URL과 패키지명을 `a11y-tools`로 변경했고, 새 저장소 생성 전용 스크립트를 기존 저장소 게시용으로 교체했습니다. GitHub Actions 빌드·배포와 `build-info.json`의 커밋 검증을 추가했습니다.

예정 주소: `https://1bobby-git.github.io/a11y-tools/` (현재 게시 완료 주소라는 의미가 아닙니다.)

## 검증 한계

Windows 스크립트의 실기 실행과 원격 Actions/Pages는 미검증입니다. axe-core는 이 환경의 네트워크 제한으로 설치하지 못했으며, CI 빌드는 설치되지 않으면 실패하도록 설정했습니다. 자동 검사만으로 법적 적합성이나 접근성 인증을 판정하지 않습니다.
