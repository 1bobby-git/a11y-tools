# 검증 범위

## 소스 이전 전 재실행한 로컬 검사

- Node.js 정적 테스트 7개 통과.
- Chromium 브라우저 회귀 테스트 45개 통과.
- 데스크톱·390px 모바일 가로 넘침, 실제 Tab 기록, 초점으로 인한 가로 넘침 및 주변 구역 변화, 기록기의 DOM 비변경을 확인했습니다.
- HTML 샌드박스의 스크립트·이벤트 차단, 보고서 문자열 비실행 렌더링, 입력 비밀번호 값 미수집을 확인했습니다.

로컬 브라우저 검사는 외부 사이트 대신 메모리로 제공한 테스트 자산을 사용했습니다. 로컬에는 axe-core 원본 패키지가 없어서 기본 엔진만 실행했습니다. 이를 실제 사이트 또는 axe-core 검사 성공으로 간주하지 않습니다.

## 이 저장소의 자동 검증

`.github/workflows/pages.yml`은 axe-core 설치 → 빌드 → 정적 검사 → Chromium 회귀 검사 → Pages 아티팩트 업로드 → 배포 → 공개 URL 확인 순서로 구성합니다. 한 단계가 실패하면 다음 배포를 진행하지 않습니다.

실제 실행 결과는 **Actions → Deploy accessibility studio**의 로그와 `accessibility-test-evidence` 아티팩트를 확인하세요. 이 문서에 적힌 절차만으로 원격 배포가 성공했다고 간주하지 않습니다.

## 검증하지 않은 범위

- 설치된 Chrome / Edge 확장 프로그램의 전체 실사이트 흐름과 브라우저별 권한 동작.
- NVDA / VoiceOver 발화·가상 커서, 모바일 보조기술.
- iframe 내부·닫힌 Shadow DOM·모든 페이지와 동적 상태.
- 모든 KWCAG / WCAG 항목, 원본 HTML 문법 전체, 법적 적합성 또는 인증.

테스트 결과와 스크린샷은 의도적으로 만든 예제 화면의 데이터이며 실제 고객 사이트의 검사 결과가 아닙니다.
