# 검증 기록

## 실제 실행한 항목

- Node.js 정적 테스트: **7개 통과** (`npm test`).
- Chromium 브라우저 테스트: **45개 통과** (`tests/verification.json`).
- UI 렌더링: 1440px 데스크톱과 390px 모바일, 페이지 가로 넘침 없음 확인.
- 실제 키보드 Tab 입력으로 초점을 이동하고, 초점으로 인해 너비가 커지는 버튼에서 가로 넘침 증가 및 주변 구역 크기·위치 변화 확인.
- 초점 관찰 전후 DOM 문자열이 동일함을 확인하여 기록기가 DOM/CSS를 변경하지 않음을 테스트.
- 초점 기록 중지 후 이벤트가 추가되지 않음을 확인.
- 입력 비밀번호 값 미수집, URL 쿼리·해시 제거, 빈 alt 및 자막 부재의 검토 상태 분리 확인.
- HTML 소스 스크립트·인라인 이벤트 차단, 외부 이미지 src 제거, same-origin 없는 샌드박스, 보고서 문자열의 비실행 렌더링 확인.
- 기본 엔진과 33개 검토 항목의 실행, 수동 근거의 보고서 반영, JSON 스키마 거부 확인.

## 환경과 제한

실행 환경의 브라우저 URL 이동이 관리자 정책으로 차단되어, 브라우저 테스트는 외부/로컬 서버에 접속하는 대신 **메모리에서 테스트 자산을 제공**했습니다. DOM·CSS 렌더링·실제 키보드 이벤트·격리 iframe·애플리케이션 코드는 Chromium에서 실행했습니다. 이 테스트를 실사이트 E2E 검사 또는 HTTP 배포 검증이라고 주장하지 않습니다.

다음은 **실행하지 못했거나 확인하지 않은 항목**입니다.

- GitHub 저장소 생성, 파일 업로드, Pages 원격 게시 및 실제 URL HTTP 200 확인.
- Windows PowerShell 배포 스크립트의 실제 실행.
- 브라우저에 확장 프로그램을 설치한 상태에서의 activeTab 권한·서비스 워커·실사이트 전체 흐름.
- axe-core 패키지 다운로드·실행. 첨부본에는 기본 규칙과 명시적 자리표시자가 포함되어 있습니다. 게시 스크립트는 axe-core 설치를 요구합니다.
- NVDA/VoiceOver 실제 발화·가상 커서, iOS/Android 보조기술, 모든 교차 출처 프레임.
- 전체 원본 HTML 문법, 모든 WCAG/KWCAG 항목, 법적 적합성·인증.

`tests/focus-evidence.json`은 테스트용 가상 화면에서 **실제로 측정한** 초점/레이아웃 기록입니다. 사용자의 사이트를 검사한 데이터가 아닙니다.

## a11y-tools 배포 준비본 재검증 — 2026-09-07

- Node.js 정적 테스트: 기존 7개 + 배포 설정 테스트 6개 = **13개 통과**.
- Chromium 메모리 픽스처 브라우저 테스트: **45개 통과**, 변경 후 재실행.
- GitHub Actions YAML 구문과 build → deploy 의존관계 확인.
- 원본 핵심 파일 core.js/focus.js/standards.js/app.js/style.css의 바이트 동일성 확인.
- Pages 하위 경로에서 참조하는 정적 자산 존재 확인.
- axe-core 미설치 상태에서 `--require-axe` 빌드가 실제 실패함을 확인.
- 대상 GitHub 저장소의 존재와 계정 쓰기 권한은 API로 확인.
- 이 세션에서는 터미널 호스트 이름 해석 실패와 원격 쓰기 도구 부재로 **업로드 및 배포를 수행하지 못함**.
- 추가한 PowerShell 스크립트는 Windows 실기에서 실행하지 못했으며, 정적 설정 테스트만 수행함.
