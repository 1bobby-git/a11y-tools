/* Optional axe-core bundle placeholder. `npm install --ignore-scripts` and
 * `npm run build` replace this file with the pinned upstream distribution.
 * This placeholder does NOT perform or claim axe-core/WCAG testing. */
globalThis.STUDIO_AXE_UNAVAILABLE = true;
